name: grid_engine
description: Core calculation engine for Grid Survival Simulator - pure Dart, no Flutter dependency
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0
